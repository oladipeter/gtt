<!-- POSITION 1 -->

<h1><%= @about_us.title %></h1>
<div class="main_yield_text">
  <%= raw @about_us.content %>
</div>

<% content_for :position_1 do %>

    <!-- ARTICLE POS 1 -->

    <% @articles.each do |article| %>

        <% if article.position == "1" %>

            <% if article.start_date.blank? %>
                <!-- If blank then set the current date -->
                <% @start_date = @current_datetime %>
            <% else %>
                <!-- Set by the user -->
               <% @start_date = article.start_date %>
            <% end %>

            <% if article.end_date.blank? %>
                <!-- If blank then set the current date + 1 year -->
                <% @end_date = @current_datetime + 1.years %>
            <% else %>
                <% @end_date = article.end_date %>
            <% end %>

            <!-------------------------- if start_date and end_date !All not blank! then show-------------------------->
            <!--------------------------------------------------------------------------------------------------------->

            <% if @start_date && @end_date %>

                <% if (@start_date <= @current_datetime) && (@end_date >= @current_datetime)  %>
                  <div>

                      <div class="item">
                        <%= image_tag article.image.url(:medium) %>
                        <h3><%= article.title %></h3>

                        <div class="article_date">
                          Létrehozva : <%= article.created_at.strftime("%F") %>
                        </div>
                        <p>
                          <%= raw truncate("#{article.short_description}", :length => 220, :omission => "...") %>
                        </p>
                        <% if article.outter_link.blank? %>
                            <div class="read_more"><%= link_to "Bővebben", website_article_path(article) %></div>
                        <% else %>
                            <div class="read_more"><%= link_to "Bővebben", "#{article.outter_link}", :target => "_BLANK" %></div>
                        <% end %>

                      </div>

                    </div>
                <% end %>
            <% end %>

        <% end %>

  <% end %>

  <!-- ARTICLE POS 1 END -->

<% end %>

<!-- POSITION 2 -->

<% content_for :position_2 do %>

    <% @articles.each do |article| %>
        <% if article.position == "2" %>

        <% if article.start_date.blank? %>
                <!-- If blank then set the current date -->
                <% @start_date = @current_datetime %>
            <% else %>
                <!-- Set by the user -->
               <% @start_date = article.start_date %>
            <% end %>

            <% if article.end_date.blank? %>
                <!-- If blank then set the current date + 1 year -->
                <% @end_date = @current_datetime + 1.years %>
            <% else %>
                <% @end_date = article.end_date %>
            <% end %>

            <!-------------------------- if start_date and end_date !All not blank! then show-------------------------->
            <!--------------------------------------------------------------------------------------------------------->

            <% if @start_date && @end_date %>

                <% if (@start_date <= @current_datetime) && (@end_date >= @current_datetime) %>
                    <div class="simple">
                      <h2><%= article.title %></h2>

                      <div class="span-2 append-1">
                        <div class="month">
                          <%= article.created_at.strftime("%b") %>
                        </div>
                        <div class="day">
                          <%= article.created_at.strftime("%d") %>
                        </div>
                      </div>
                      <div class="span-13 last">
                        <div class="text">
                          <%= raw truncate("#{article.short_description}", :length => 200, :omission => "...") %>
                        </div>
                      </div>
                      <div class="span-3 prepend-13">
                        <% if article.outter_link.blank? %>
                            <div class="read_more"><%= link_to "Bővebben", website_article_path(article) %></div>
                        <% else %>
                            <div class="read_more"><%= link_to "Bővebben", "#{article.outter_link}", :target => "_BLANK" %></div>
                        <% end %>
                      </div>
                    </div>
                <% end %>
            <% end %>
        <% end %>
    <% end %>

<% end %>

<!-- POSITION 3 -->

<% content_for :position_3 do %>

    <% @articles.each do |article| %>
        <% if article.position == "3" %>

            <% if article.start_date.blank? %>
                <!-- If blank then set the current date -->
                <% @start_date = @current_datetime %>
            <% else %>
                <!-- Set by the user -->
                <% @start_date = article.start_date %>
            <% end %>

            <% if article.end_date.blank? %>
                <!-- If blank then set the current date + 1 year -->
                <% @end_date = @current_datetime + 1.years %>
            <% else %>
                <% @end_date = article.end_date %>
            <% end %>

            <!-------------------------- if start_date and end_date !All not blank! then show-------------------------->
            <!--------------------------------------------------------------------------------------------------------->

            <% if @start_date && @end_date %>

                <% if (@start_date <= @current_datetime) && (@end_date >= @current_datetime) %>
                    <div class="position_3">
                      <h2><%= article.title %></h2>

                      <div class="pos3_short_description">
                        <%= image_tag article.image.url(:thumb) %>
                        <%= raw truncate("#{article.short_description}", :length => 250, :omission => "...") %>
                      </div>
                      <% if article.outter_link.blank? %>
                          <div class="read_more"><%= link_to "Bővebben", website_article_path(article) %></div>
                      <% else %>
                          <div class="read_more"><%= link_to "Bővebben", "#{article.outter_link}", :target => "_BLANK" %></div>
                      <% end %>
                    </div>
                <% end %>
            <% end %>
        <% end %>
    <% end %>

<% end %>

<!-- POSITION 4 -->

<% content_for :position_4 do %>

    <% @articles.each do |article| %>
        <% if article.position == "4" %>

            <% if article.start_date.blank? %>
                <!-- If blank then set the current date -->
                <% @start_date = @current_datetime %>
            <% else %>
                <!-- Set by the user -->
                <% @start_date = article.start_date %>
            <% end %>

            <% if article.end_date.blank? %>
                <!-- If blank then set the current date + 1 year -->
                <% @end_date = @current_datetime + 1.years %>
            <% else %>
                <% @end_date = article.end_date %>
            <% end %>

            <!-------------------------- if start_date and end_date !All not blank! then show-------------------------->
            <!--------------------------------------------------------------------------------------------------------->

            <% if @start_date && @end_date %>

                <% if (@start_date <= @current_datetime) && (@end_date >= @current_datetime) %>
                    <div class="position_4">
                      <h2><%= article.title %></h2>

                      <div class="pos4_short_description">
                        <%= raw truncate("#{article.short_description}", :length => 200, :omission => "...") %>
                      </div>
                      <% if article.outter_link.blank? %>
                          <div class="read_more"><%= link_to "Bővebben", website_article_path(article) %></div>
                      <% else %>
                          <div class="read_more"><%= link_to "Bővebben", "#{article.outter_link}", :target => "_BLANK" %></div>
                      <% end %>
                    </div>
                <% end %>
            <% end %>
        <% end %>
    <% end %>

<% end %>

<!-- POSITION 5 -->

<% content_for :position_5 do %>

    <% @articles.each do |article| %>
        <% if article.position == "5" %>
            <% if article.outter_link.blank? %>
                <%= link_to "#{article.title}", website_article_path(article) %>
            <% else %>
                <%= link_to "#{article.title}", "#{article.outter_link}", :target => "_BLANK" %>
            <% end %>
        <% end %>
    <% end %>

<% end %>

<!-- ADVICES - KÖZLEMÉNYEK -->

<% content_for :advices do %>

  <%= raw @commercial.content %><br/>

  <% @advices.each do |advice| %>
    <div class="advices">
      <h2><%= advice.title %></h2>
      <%= raw truncate("#{advice.content}", :length => 200, :omission => "...") %>
      <div class="read_more">
        <%= link_to "Bővebben", website_advice_path(advice) %>
      </div>
    </div>
  <% end %>

<% end %>