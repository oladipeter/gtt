<% content_for :faqs do %>
  <h2>FAQ (<%= "#{@quantity_faqs}" %> db)</h2>
  <div class="article_text">

    <% if @faqs.empty? %>

      <%= "Jelenleg nincs FAQ, ehhez a rendszerhez!" %>

    <% else %>

      <% @faqs.each do |faq| %>
        <strong><%= faq.title %></strong><br/><span class="created">Létrehozva : </span><%= faq.created_at %><br/><br/>
        <div class="faq_description"><%= raw truncate ("#{faq.description}"), :length => 100, :omission => '...' %></div>
      <% end %>

      <%= link_to "Megnézem az összes kérdést!", "#", :class => "all" %>

    <% end %>
  </div>

<% end %>